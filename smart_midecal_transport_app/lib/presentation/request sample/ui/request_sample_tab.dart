import 'package:flutter/material.dart';

class RequestSampleTab extends StatelessWidget {
  const RequestSampleTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}